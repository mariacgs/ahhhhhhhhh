package first;

public interface Generator
{

    // functional interface: one method
    //lambda is there to shorten conde
    //fun interface and its method will ask 2 param it doesnt define what it does ill define it in a class with lambda

    public void DoMath(int a, int b);

}
